{
  "name": "not-my-reforge",
  "version": "2.5.7",
  "description": "not oh-my. reforged. — AI agent orchestration, quality hooks, and smart routing for Claude Code",
  "author": "Tom (speson)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "npx tsx --test src/lib/__tests__/*.test.ts",
    "release": "./scripts/release.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
